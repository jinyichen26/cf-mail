{
  "name": "cloudflare-mailbox-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "db:migrate": "wrangler d1 migrations apply mailbox-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mailbox-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "resend": "^4.0.1",
    "postal-mime": "^2.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.7.2"
  }
}
